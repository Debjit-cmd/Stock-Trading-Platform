import React from 'react';
import {render,screen} from '@testing-library/react' ; //on every change rendering
import '@testing-library/jest-dom/extend-expect'; // done to delay the check (in millisec)

